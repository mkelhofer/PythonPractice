import random #import modules you need, doesn't automatically to keep running light ---> import what you need

keep_playing = True #serves as flag/switch (off or on)
while keep_playing == True;
	correct_number = random.randint(1,10);
	print correct_number
	#print guess
	keep_guessing = True
	total_tries = 5
	used_tries - 0;
	while keep_guessing == True:
		guess = int(raw_input("Guess a number between 1-10\n"))
		# guess = raw_input
		try:
			convert_guess = int(guess)
		expect



